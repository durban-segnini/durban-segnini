<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8"/>
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <meta name="robots" content="noindex, nofollow">
    <title>{{ title or "Durban Segnini Gallery" }}</title>
    <link rel="stylesheet" href="{{ '/assets/css/styles.css' | url }}">
    <link rel="stylesheet" href="css/hero-index.css"/>
    {% if pageCss %}
      <link rel="stylesheet" href="{{ '/assets/css/pages/' | url }}{{ pageCss }}">
    {% endif %}
  </head>
  <body>
    <div class="page-wrapper">
      {% if isHome %}
        {% include "partials/header_home.njk" %}
      {% else %}
        {% include "partials/header.njk" %}
      {% endif %}
        {{ content | safe }}
      {% include "partials/footer.njk" %}
      <script src="{{ '/assets/scripts/mobile-menu.js' }}" defer></script>
      {% if pageScript %}
        <script src="{{ '/assets/scripts/' | url }}{{ pageScript }}" defer></script>
      {% endif %}
    </div>
  </body>
</html>